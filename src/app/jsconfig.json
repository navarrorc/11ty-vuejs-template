{
    "compilerOptions": {
        "baseUrl": "../../.",  // all paths are relative to the baseUrl
        "paths": {
            "@/*" : ["src/*"]   // resolve any `~/foo/bar` to `<baseUrl>/foo/bar`
        }
    }
}